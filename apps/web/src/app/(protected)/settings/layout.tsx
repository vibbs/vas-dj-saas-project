/**
 * Settings Layout
 * No additional layout needed - using unified MainSidebar from protected layout
 */
export default function SettingsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
